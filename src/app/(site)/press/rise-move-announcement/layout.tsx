import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'RISE™ Announces RISE™ Move — Bidirectional Staircase Navigation',
  description:
    'RISE™ announces the Move platform — bidirectional staircase navigation for the Push Smart Adjustable Base. Both directions. We are doing something about the stairs.',
}

export default function MoveAnnouncementLayout({ children }: { children: React.ReactNode }) {
  return children
}
